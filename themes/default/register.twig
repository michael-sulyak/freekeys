{% extends 'base.twig' %}

{% block title %}Регистрация{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-lg-3">			
			{{ include('banner_1.twig') }}	
		</div>

		<div class="col-lg-6 text-center">
			<h1 class="text-uppercase">Регистрация</h1>
			<p>Логин (ID) для входа на сайт вам выдадут автоматически после нажатия кнопки "Зарегистрироватся".</p>

			<form method="post" role="form">
				<div class="form-group text-left">
					<label>Логин</label>
					<input type="text" name="interface[auth][register][0][login]" class="form-control" required>
				</div>

				<div class="form-group text-left">
					<label>Придумайте пароль</label>
					<input type="password" name="interface[auth][register][0][password]" class="form-control" required>
				</div>

				<div class="form-group text-left">
					<label>Повторите пароль</label>
					<input type="password" name="interface[auth][register][0][password2]" class="form-control" required>
				</div>
				
				<div class="form-group text-left">
					<label>E-Mail</label>
					<input type="text" name="interface[auth][register][0][email]" class="form-control" required>
				</div>

				<div class="form-group text-left">
					<label>Введите код с картинки</label>
				    <input type="text" class="form-control" name="interface[auth][register][0][captcha]" pattern="[a-zA-Z0-9]{4}" autocomplete="off" required>
				</div>

				{% set captcha_id = random() %}
				<input type="hidden" name="interface[auth][register][0][captcha_id]" value="{{ captcha_id }}">
				
				<p>
					<a href="#" onclick="document.getElementById('captcha').src='{{ url_base }}/includes/helper/captcha/image.php?id={{ captcha_id }}&amp;'+Math.random(); return false;" title="Обновить">
						<img src="{{ url_base }}/includes/helper/captcha/image.php?id={{ captcha_id }}" id="captcha" class="captcha">
					</a>
				</p>

				<button type="submit" class="btn btn-lg btn-primary btn-block" name="interface[auth][register][0][run]">Зарегистрироватся</button>
			</form>
		</div>

		<div class="col-lg-3">			
			{{ include('banner_2.twig') }}	
		</div>
	</div>
{% endblock %}